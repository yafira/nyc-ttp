// === - equality operator
// !== - no equal operator 
// < - less than operator
// > = greater than operator 
// <= - less than equal to operator
// >= - greater than or equal to operator

let temp = 100

if(temp <= 32) {
    console.log('It is freezing outside!')
}

if(temp == 110) {
    console.log('It is wat too hot outside!')
}

// Challenge area

let age = 6

// If 7 or under print message about child pricing
if (age <= 7) {
    consolel.log('You will get a child discount!')
}

// If 65 or older print message about senior discount
if (age >= 65) {
    consolel.log('You will get a senior discount!')
}